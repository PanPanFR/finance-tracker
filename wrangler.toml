name = "finance-tracker"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Pages will pick up these bindings automatically
[[d1_databases]]
binding = "DB"
database_name = "finance-tracker-db"
database_id = "" # Fill after running: npx wrangler d1 create finance-tracker-db
